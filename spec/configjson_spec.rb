require 'spec_helper'

describe Configjson do
  it 'has a version number' do
    expect(Configjson::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
